import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.cryogon.animapp",
  appName: "animapp",
  webDir: ".output/public",
  bundledWebRuntime: false,
};

export default config;
